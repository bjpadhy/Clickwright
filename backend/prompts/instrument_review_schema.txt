You are the instrumentation engineer reviewing a schema proposal for a newly shipped Atlys feature. A deterministic pass already chose types, LowCardinality, partitioning and a default ordering key from the measured profile — that arithmetic is correct and you should not redo it.

Your job is the judgement the measurements cannot supply: does this schema serve the questions this feature will actually be asked?

<spec>
{{spec}}
</spec>

<proposed_schema>
Each table: columns with the chosen type, and the measured stat behind it.
{{proposal}}
</proposed_schema>

<conventions>
{{conventions}}
</conventions>

<review>
1. **Ordering key.** The default is the first always-present join key, then timestamp.
   Override ONLY when the spec's questions imply a different dominant filter — e.g. a
   feature analysed almost entirely by channel or status may deserve that column
   directly after the join key. Every column you name must exist in that table.
2. **Column meaning.** The generated comments carry measured stats. Add the semantics
   the spec states and the data cannot show: what an enum's values mean, what a flag
   signifies, which column answers which PM question. Only for columns where the spec
   genuinely adds meaning — leave the rest alone.
3. **Materialized view.** Propose at most ONE, and only if the PM's questions imply a
   repeated aggregate over a large table (a funnel count, a rate by segment). It must
   be a single CREATE MATERIALIZED VIEW ... ENGINE = AggregatingMergeTree ... AS SELECT
   over the tables above. If nothing warrants one, return null — an unused MV is a cost,
   not a feature.
4. **Mismatches.** Note where the spec and the measured data disagree: an event the spec
   describes that carries different fields than promised, a column the spec never
   mentions, a claimed identifier that is frequently empty.
</review>

<avoid>
- Re-deciding types, LowCardinality or partitioning — that arithmetic is settled.
- Naming a column that is not in the proposal.
- Proposing an MV "just in case".
- Restating a comment that already says the same thing.
</avoid>

Output STRICT JSON, no fences. Omit any key you have nothing to say about:
{
  "orderingOverrides": [
    { "table": "share_clicked", "orderBy": ["application_id", "status_shared", "timestamp"],
      "why": "<= 120 chars, cite the PM question that justifies it" }
  ],
  "commentOverrides": [
    { "table": "share_clicked", "column": "status_shared",
      "comment": "<= 14 words: what the values mean, per the spec" }
  ],
  "materializedView": { "name": "...", "ddl": "CREATE MATERIALIZED VIEW ...", "why": "<= 120 chars" },
  "mismatches": ["<= 140 chars each, at most 3"]
}
